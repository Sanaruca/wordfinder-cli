{
  "name": "wordfinder-cli",
  "author": "sanaruca",
  "version": "1.0.0",
  "license": "MIT",
  "bin": {
    "wfind": "./bin/index.js"
  },
  "scripts": {
    "build": "tsc --project ./tsconfig.json",
    "install-globally": "npm i && npm run build && npm i -g"
  },
  "dependencies": {
    "rae-api": "1.5.0"
  },
  "devDependencies": {
    "@types/node": "18.0.0",
    "typescript": "^4.7.4"
  }
}
